- name: "Practicing the Ansible play books"
  hosts: localhost
  become: yes
  tasks:
  - name: printing the names
    ansible.builtin.package:
      name: "{{ item }}"
      state: latest
    loop:
    - nginx
    - nodejs
    - mysql
    - zip
